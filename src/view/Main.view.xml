<mvc:View
	controllerName="be.wl.externallib.controller.Main"
	displayBlock="true"
	xmlns="sap.m"
	xmlns:mvc="sap.ui.core.mvc">

	<MessagePage
		title="{i18n>appTitle}"
		text="{i18n>appTitle}"
		icon="sap-icon://accept"
		id="page"
		description="{/demo}">
		<buttons>
			<Button
				text="{i18n>btnText}"
				press="sayHello" />
		</buttons>
	</MessagePage>

</mvc:View>